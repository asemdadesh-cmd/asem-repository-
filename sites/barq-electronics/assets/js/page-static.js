/* Pages whose content is entirely static markup (about, 404) still need the
   shared shell: header state, theme, language, drawers and scroll motion. */
import { initShell } from './ui.js';
initShell();
